import HeaderStyled from "./HeaderStyled";

const Header = (): React.ReactElement => {
  return (
    <HeaderStyled>
      <h1 className="heading-title">Tuttyfly</h1>
    </HeaderStyled>
  );
};

export default Header;
